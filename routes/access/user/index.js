const all = require("./all")
const register = require("./register")
const detail = require("./edit")
const update = require("./update")
const deleted = require("./destroy")

module.exports = {
    all,
    register,
    detail,
    update,
    deleted
}
